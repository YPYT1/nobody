import { reloadable } from "../../../utils/tstl-utils";

/** 自定义防御系统 
 *  减伤,增伤
 */
@reloadable
export class CustomDefense {

    InitUnitArmorSystem(hUnit: CDOTA_BaseNPC) {
        // hUnit.ReductionPct
    }
}